# Example environment variables for Vercel or local development
# Either set DATABASE_URL or set the PG* variables below

# Option A: single connection string
# DATABASE_URL=postgresql://username:password@host:5432/database

# Option B: individual variables
PGHOST=your-db-host
PGPORT=5432
PGDATABASE=your_database
PGUSER=your_db_user
PGPASSWORD=your_db_password

# Optional: override table default
DEFAULT_TABLE_NAME=hospital_records
